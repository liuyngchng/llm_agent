# config.yaml
model:
    #  模型加载路径
    path: /data/DeepSeek-R1-Distill-Qwen-7B
    # 模型名称
    name: deepseek-r1
    # 访问模型 API 的 key
    key: your-secret-key
    # 当本地没有 GPU 资源时， 设置为 True， 启用 Mock 模式，将返回假数据
    is_mock: true